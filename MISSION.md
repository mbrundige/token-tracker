# Mission: Open-source contrib via token-tracker landing redesign

## Why
Help Max ship better public work on [mbrundige/token-tracker](https://github.com/mbrundige/token-tracker) while learning safe GitHub contribution. First win: a stronger GitHub landing page (README hero, logos, screenshots) as a fork PR Max can review.

## Success looks like
- Open a clean presentation-only PR (README + `docs/logos` + `docs/screenshots`) into Max's repo
- Explain fork → branch → push → PR in plain words, with AI doing the git mechanics
- Later: use the same loop for bigger architecture experiments without breaking Max's main

## Constraints
- Teach with flowcharts first; sixth-grade reading level; no long specs
- This worktree = landing redesign only; leave `scripts/` / `bin/` / `templates/` alone
- Push only to BowmanStephen fork (`origin`); never push `upstream`

## Out of scope
- Rewriting core ledger / Rust port / install behavior in this learning track
- Building a separate marketing website outside GitHub README
